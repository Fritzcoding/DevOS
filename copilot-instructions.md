# DevOps Lite Copilot Entry Point

The canonical agent instructions live in `.github/AGENTS.md`.

This root file exists only for tools that discover repository instructions from
the project root. Do not duplicate rules here; update `.github/AGENTS.md` or the
focused files under `.github/instructions/` instead.
